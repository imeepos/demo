import { describe, expect, it } from 'vitest'; import {
{{camelCase name}}
} from './{{camelCase name}}'; describe('{{camelCase name}}', () => { it('should
work correctly', () => { const input = 'test'; const result =
{{camelCase name}}(input); expect(result).toBe(input); }); // Add more tests
here });