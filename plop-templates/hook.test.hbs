import { renderHook } from '@testing-library/react'; import { describe, expect,
it } from 'vitest'; import { use{{pascalCase name}}
} from './use{{pascalCase name}}'; describe('use{{pascalCase name}}', () => {
it('should initialize with default values', () => { const { result } =
renderHook(() => use{{pascalCase name}}());
expect(result.current.value).toBe(''); expect(typeof
result.current.setValue).toBe('function'); }); // Add more tests here });